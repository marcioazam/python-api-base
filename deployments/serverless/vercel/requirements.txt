# Vercel Python dependencies
# Copy to project root as requirements.txt when deploying

# Core framework
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Async support
httpx>=0.27.0
anyio>=4.4.0

# Note: Vercel handles most dependencies automatically
# Include only what's needed for the API
